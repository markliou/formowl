{
  "name": "formowl",
  "build": {
    "dockerfile": "../containers/dev/Dockerfile",
    "context": ".."
  },
  "workspaceFolder": "/workspace",
  "remoteEnv": {
    "PYTHONPATH": "/workspace/python",
    "FORMOWL_DATA_DIR": "/workspace/.formowl/data"
  },
  "postCreateCommand": "python -m pip install -e \".[dev]\" && pre-commit install && python -m unittest discover -s tests"
}
