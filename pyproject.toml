[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "formowl"
version = "0.1.0"
description = "Source-preserving knowledge management MCP servers."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "Authlib>=1.6,<2",
  "cryptography>=44,<47",
  "httpx>=0.28,<1",
  "jieba>=0.42.1,<0.43",
  "mcp==1.28.1",
  "psycopg[binary]>=3.2,<4",
  "sentencepiece>=0.2,<0.3",
  "starlette>=0.37,<1",
  "uvicorn>=0.34,<1"
]

[project.optional-dependencies]
dev = [
  "coverage[toml]>=7.6",
  "jieba>=0.42.1,<0.43",
  "neo4j==5.28.1",
  "pre-commit>=4.0",
  "ruff==0.8.6",
  "sentencepiece>=0.2,<0.3"
]
graph-adapters = [
  "rapidfuzz>=3.14,<4",
  "splink>=4,<5"
]
issue56-dense-cpu = [
  "huggingface-hub==0.26.5",
  "numpy==1.26.4",
  "safetensors==0.4.5",
  "sentence-transformers==3.3.1",
  "tokenizers==0.20.3",
  "torch==2.5.1",
  "transformers==4.46.3"
]
mail-experiments = [
  "jieba>=0.42.1,<0.43",
  "sentencepiece>=0.2,<0.3"
]

[project.scripts]
formowl-container-entrypoint = "formowl_gateway.container_entrypoint:main"
formowl-connected-mcp = "formowl_gateway.runtime:main"
formowl-issue20-evidence = "formowl_evidence.issue20_packet:main"
formowl-kg-eval = "formowl_kg_eval.cli:main"
formowl-semantic-mcp-jsonrpc = "formowl_gateway.cli:main"
formowl-project-mcp-jsonline-compat = "formowl_project_mcp.server:main"
formowl-wiki-mcp-jsonline-compat = "formowl_wiki_mcp.server:main"

[tool.setuptools.packages.find]
where = ["python"]

[tool.setuptools.package-data]
"formowl_core" = [
  "tokenizer_profiles/jieba_sentencepiece_frozen_profile_candidate_admission_v1/calibration_corpus.txt",
  "tokenizer_profiles/jieba_sentencepiece_frozen_profile_candidate_admission_v1/jieba_user_dictionary.txt",
  "tokenizer_profiles/jieba_sentencepiece_frozen_profile_candidate_admission_v1/manifest.json",
  "tokenizer_profiles/jieba_sentencepiece_frozen_profile_candidate_admission_v1/sentencepiece.model",
  "tokenizer_profiles/jieba_sentencepiece_frozen_profile_candidate_admission_v1/sentencepiece.vocab"
]
"formowl_graph.storage" = ["migrations/*.sql"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["python"]

[tool.coverage.run]
branch = true
source = [
  "python"
]

[tool.coverage.report]
fail_under = 85
show_missing = true
skip_covered = false

[tool.ruff]
line-length = 100
target-version = "py311"
src = [
  "python",
  "tests"
]

[tool.ruff.lint]
select = [
  "E4",
  "E7",
  "E9",
  "F"
]
