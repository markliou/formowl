{
  "name": "formowl",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b packages/formowl-contract packages/project-mcp packages/wiki-mcp",
    "typecheck": "tsc -p tsconfig.typecheck.json --pretty false",
    "precommit": "pre-commit run --all-files"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.5.0"
  }
}
